# SEO対策ツール リスクチェック機能（ファクトチェック・コピペチェック）開発

<!--
  壁打ち（/resume-interview）で聞き取った内容を記録する一次情報源。
  ここに書かれていないことは、レジュメには書かれません。
  聞けていない項目には未ヒアリングの印（HTMLコメントで「未ヒアリング」）を残し、
  推測で埋めないでください。
-->

## 参加した年

2025年頃

## 参加期間

約2か月

## 担当工程

要件定義、設計（UI/UXデザイン含む）、コーディング（フロントエンド・バックエンド）、テスト

## 経験した職種

デザイン、フロントエンド、バックエンド

## 実際に使っていた技術

- フロントエンド: Vue.js（Vue2）
- バックエンド: Laravel（PHP）
- AI/LLM: Google Gemini（`2.5 Flash`）
- デザインツール: Figma

## ① どんなサービス・システムか

[SEO対策ツール 記事作成機能 UIリニューアル](./2025-seo-article-ui-renewal.md)と同じサービス・同じチームでの後続案件。
toB向けSaaS、SEO担当者をメインターゲットとする。

記事作成機能はAIによって記事を生成する仕組みだったため、生成された記事をそのまま信頼して公開できるか
不安だという声がクライアントから上がっていた。これに対応するためのリスクチェック機能で、
次の2つから構成される。

- **ファクトチェック機能**: 段落ごとに、記事の文章内容が事実に基づいているか（嘘偽りがないか）をAIで判定する
- **コピペチェック機能**: 記事の文章が他サイトからの丸ごとの引用・コピーでないかを判定する

### 利用規模

リリース後、徐々に実行回数が増え、半年以上前からは月間でファクトチェック約1,700回・コピペチェック約1,200回
という規模で安定して推移している。記事作成機能を利用しているユーザーは、ほぼ全員がこのリスクチェック
（ファクトチェック・コピペチェックのいずれも）を利用している。

## ② チーム構成と、自分の担当

体制は前回（記事作成機能UIリニューアル）と同じ3名だが、デザイナーは完全に離脱。

- プロジェクトマネージャー: 1名
- エンジニア: 2名（本人含む）
- デザイナー: 0名（前回参加していたデザイナーも今回は完全に不在）

### チームとして行ったこと

- ファクトチェック機能・コピペチェック機能の企画から実装・リリースまで
- 記事作成フローに深く組み込む形での機能提供（競合はコピーアンドペーストで試すだけの簡易ベータ版だった）

### 自分自身が行ったこと

- UI/UXデザイン作成、ユーザーインタビュー実施（デザイナー不在のため、デザイン全体を主導。
  前回いたデザイナーはあくまで相談相手程度で、主体は本人だった）
- 要件定義主導（判定基準の設計、自動化範囲の決定など）
- バックエンド実装（LLM連携、キュー・ポーリング処理、LLM呼び出しの薄いラッパー実装）
- テスト（もう1名のエンジニアと共同）

役割分担: 実装は本人がバックエンド、もう1名のエンジニアがフロントエンドを担当。
要件定義・設計・デザインは本人が主導。テストは2名で実施。

## ③ 自分が使った技術と、その使い方

- **Google Gemini（`2.5 Flash`）**: ファクトチェックの判定ロジックに使用。日本ファクトチェックセンターの
  ファクトチェック定義・規定を参考にプロンプトを設計し、判定基準として組み込んだ。判定理由も文章で
  提示させ、その理由をもとに記事を再生成できる仕組みも合わせて実装した。
- **Laravel（PHP）**: バックエンド実装を担当。判定処理は1回あたり数分〜10分弱かかるため、
  キューに投入してポーリングする方式で非同期処理を実装。
  また、プロダクト全体で記事作成・情報整理などに複数のLLM/モデルを都度使い分けていた。そのため「どのモデルを使うか」「どのレスポンス形式か」「どのコンテキストを渡すか」を
  クラスベースで設定し、それを渡すだけでLLM APIを実行できる薄いラッパーを新たに実装した。
- **Figma**: UI設計、ワイヤーフレーム〜プロトタイプ作成、ユーザーインタビューに使用（前回案件からの継続）。

## ④ 開発した機能

- **ファクトチェック機能**: 記事を段落ごとにAI（`Gemini 2.5 Flash`）で解析し、日本ファクトチェックセンターの
  定義を参考にしたプロンプトで事実性を判定。判定理由を提示し、理由に基づいた記事の再生成が可能。
- **コピペチェック機能**: 文章を形態素解析し、文字列単位でGoogle検索をかけて既存サイトの文章と一致するか
  どうかを確認するロジック（既存の社内サービスのロジックを流用、本人による新規実装ではない）。
- 判定はすべて自動で実行し、判定結果を見て記事を修正するかどうかはユーザーに一任する設計。

## ⑤ 取り組んだ課題と、その背景

- 記事作成機能はAIが生成する記事であり、クライアントから「そのまま公開してよいか不安」という声が
  上がっていた。
- 一部競合がファクトチェック機能をベータ版として出しはじめていた時期であり、競合は文章をコピーアンドペースト
  して試すだけの簡易的な提供形態だった。これに対し、記事作成フローに深く組み込んだ形で提供することで
  ユーザーが違和感なく使える体験を作り、差別化を図りたかった。

## ⑥ 工夫した点と、そう判断した理由

- **ファクトチェックにAIを採用した理由**: 記事作成後に何日もかけるのではなく、数十分〜十分程度で
  完了させたい要件があったため。厳密なファクトチェックではなく「明確な嘘がないか」をざっと確認できる
  精度を想定していたため、外部の厳密なファクトチェックサービスは検証すら行わず、コスト面からもAIを選択した
- **コピペチェックは既存の社内サービスを流用**: 社内にすでに同種のロジックが存在していたため、新規開発は行わずそれを利用した
- **LLM呼び出しの薄いラッパーを新規実装**: プロダクト全体で複数のLLM/モデルをドメインロジックに
  直接紐づけて使っており、モデル・レスポンス形式・コンテキストの管理が煩雑になっていた。
  クラスベースで設定を渡すだけでLLM APIを実行できる仕組みを作ることで、この煩雑さを解消した
- **キュー＋ポーリングによる非同期処理**: 判定処理に数分〜10分弱かかるため、同期処理ではなくキューに
  投入してポーリングする設計を採用した

## ⑦ 成果

- 同時期にリリースしたUIリニューアルを含めた成果として、受注率が32%から36%（4ポイント改善）。
- 展示会でファクトチェック・コピペチェック機能を見せたことがきっかけでSQL（Sales Qualified Lead）を
  獲得できた例もあった。
- 解約率について、目標に対する実績で126%達成。解約額としては60万円だったものが45万円まで改善した（これはUIリニューアルを含めた成果であり、リスクチェック機能単体の効果とは切り出せていない）。
- ファクトチェック機能・コピペチェック機能単体で、月2件の受注アップに貢献。
- 展示会でも、競合と比較して明確な優位に立てる状況だという声を得た。
- AIが生成した記事を信用しきれず、クライアントの決裁者を説得できないまま解約となっていたケースへ
  アプローチできるようになった。

## 改善の余地があった点・反省

タイトなスケジュールの中、デザイン仕様の確定の遅れと品質基準の未確定によって、開発全体の工程を
圧迫することになった。反省点は次の2点。いずれも「反省点 → 原因 → 次があるならどうするか」で整理する。

- **デザイン仕様の確定が遅れた**。原因は、ステークホルダーからのフィードバックによる差し戻しが多く、
  仕様が二転三転したこと。さらにその根本には、周りの意見を聞きすぎて自分（デザイン主導者）の軸が
  ぶれてしまったことがある。次があるなら、まず自分の軸をしっかり持ち、ユーザーインタビューで得た
  顧客の声を踏まえた上で、社内ヒアリングでステークホルダーの意見を収集して合意を取りに行く。
  意見に流されて軸をぶらさない。
- **品質基準を決めないまま着手した**。原因は、プロジェクト開始時のリスク洗い出しで品質チェックの
  観点自体が漏れており、「AIの出力をどの精度・基準で許容するか」というチェック機構の品質基準が
  未定義のままだったこと。次があるなら、こうしたチェック機構の品質基準をプロジェクト開始前に
  明確に決めてから着手する。

## 学び

- 上記2点は案件固有ではなく、プロジェクトの進め方全般に効く学びと捉えている。次回以降の原則として、
  (1) 自分の軸を持ってから顧客の声を得て、その上でステークホルダーとの合意を取りに行く、
  (2) 品質基準を明確にしてから着手する、の2つを徹底したい
